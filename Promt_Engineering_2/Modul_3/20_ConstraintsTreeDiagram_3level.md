# Схема: «Классификация ограничений в промптах» (3-уровневая компоновка)

```mermaid
%%{init: {
  "theme": "base",
  "themeVariables": {
    "fontSize": "13px",
    "fontFamily": "sans-serif"
  }
}}%%
graph LR
    ROOT["ОГРАНИЧЕНИЯ\nВ ПРОМПТАХ"]

    ROOT --> V1["ОБЪЁМ ТЕКСТА"]
    ROOT --> V2["СТИЛЬ ИЗЛОЖЕНИЯ"]
    ROOT --> V3["ТОН"]
    ROOT --> V4["СТРУКТУРА ОТВЕТА"]
    ROOT --> V5["ЦЕЛЕВАЯ АУДИТОРИЯ"]
    ROOT --> V6["КЛЮЧЕВЫЕ СЛОВА"]
    ROOT --> V7["ЗАПРЕЩЁННЫЕ ТЕМЫ"]

    V1 --> V1a["300–400 слов"]
    V1 --> V1b["max 500 токенов"]
    V1 --> V1c["5 абзацев"]

    V2 --> V2a["Научный"]
    V2 --> V2b["Публицистический"]
    V2 --> V2c["Разговорный"]

    V3 --> V3a["Дружелюбный"]
    V3 --> V3b["Нейтральный"]
    V3 --> V3c["Мотивирующий"]

    V4 --> V4a["Подзаголовки"]
    V4 --> V4b["Список"]
    V4 --> V4c["Таблица"]

    V5 --> V5a["Возраст"]
    V5 --> V5b["Профессия"]
    V5 --> V5c["Уровень знаний"]

    V6 --> V6a["Обязательные"]
    V6 --> V6b["Запрещённые"]

    V7 --> V7a["Политика"]
    V7 --> V7b["Религия"]

    classDef root fill:#1565c0,stroke:#0d47a1,stroke-width:4px,color:#fff
    classDef level1 fill:#e3f2fd,stroke:#1565c0,stroke-width:4px,color:#0d47a1
    classDef level2 fill:#fff3e0,stroke:#f57c00,stroke-width:4px,color:#e65100

    class ROOT root
    class V1,V2,V3,V4,V5,V6,V7 level1
    class V1a,V1b,V1c,V2a,V2b,V2c,V3a,V3b,V3c,V4a,V4b,V4c,V5a,V5b,V5c,V6a,V6b,V7a,V7b level2
```

**Рисунок.** «Классификация ограничений в промптах» (3-уровневая компоновка). Уровень 1: корневой узел слева. Уровень 2: 7 блоков в среднем ряду. Уровень 3: 2–3 примера справа напротив каждого блока. Компактная 1:1, текст полный, цвета и рамки 4px.