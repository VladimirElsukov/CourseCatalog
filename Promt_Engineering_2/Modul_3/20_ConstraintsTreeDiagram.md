# Схема: «Классификация ограничений в промптах» (вертикальная компоновка)

```mermaid
%%{init: {
  "theme": "base",
  "themeVariables": {
    "fontSize": "14px",
    "fontFamily": "sans-serif"
  }
}}%%
graph LR
    ROOT["ОГРАНИЧЕНИЯ\nВ ПРОМПТАХ"]

    ROOT --> V1["ОБЪЁМ ТЕКСТА\n(слов, абзацев, токенов)"]
    ROOT --> V2["СТИЛЬ ИЗЛОЖЕНИЯ\n(научный, публицистический)"]
    ROOT --> V3["ТОН\n(дружелюбный, нейтральный)"]
    ROOT --> V4["СТРУКТУРА ОТВЕТА\n(подзаголовки, список, таблица)"]
    ROOT --> V5["ЦЕЛЕВАЯ АУДИТОРИЯ\n(возраст, профессия, уровень)"]
    ROOT --> V6["КЛЮЧЕВЫЕ СЛОВА\n(обязательные, запрещённые)"]
    ROOT --> V7["ЗАПРЕЩЁННЫЕ ТЕМЫ\n(политика, религия)"]

    classDef root fill:#1565c0,stroke:#0d47a1,stroke-width:4px,color:#fff
    classDef blocks fill:#e3f2fd,stroke:#1565c0,stroke-width:4px,color:#0d47a1

    class ROOT root
    class V1,V2,V3,V4,V5,V6,V7 blocks
```

**Рисунок.** «Классификация ограничений в промптах» (вертикальная компоновка). Корневой узел слева, 7 блоков один над другим. Компактная 1:1, текст полный, цвета и рамки 4px.